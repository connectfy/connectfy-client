version: '3.8'

services:
  client:
    build:
      context: .
      dockerfile: Dockerfile.prod
    image: connectfy/client-prod
    container_name: client-prod
    ports:
      - "4800:4800"
    networks:
      - app-network
    restart: always

networks:
  app-network:
    driver: bridge
